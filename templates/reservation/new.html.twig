{% extends 'base.html.twig' %}

{% block title %}Ajouter une réservation{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4">Ajouter une réservation</h1>

    <div class="card p-4 shadow-sm mb-3">
        {{ include('reservation/_form.html.twig', {'button_label': 'Ajouter'}) }}
    </div>

    <a class="btn btn-secondary" href="{{ path('app_reservation_index') }}">Retour à la liste</a>
</div>
{% endblock %}
